/** TODO
 * Should be able to update a existing user
 * Should not be able to update a user that does not exist
 * Should not create a new user, but update an existing one
 */

describe('CreateUser', () => {
  it('should be able to create a new user', async () => {
    console.log('XESQUE')
  })
})
